buildscript {
  ext {
    // centralize versions if you like
  }
  repositories {
    google()
    mavenCentral()
  }
  dependencies {
    // NOTE: No com.android.tools.build:gradle here anymore in modern setups
  }
}

allprojects {
  repositories {
    google()
    mavenCentral()
    maven { url = uri("$rootDir/../node_modules/react-native/android") }
  }
}

tasks.register("clean", Delete) {
  delete rootProject.buildDir
}

